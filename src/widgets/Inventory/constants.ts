import { TInventory } from '@shared/types';

export const initialAvailableCellState: TInventory<boolean> = {
	helmet: false,
	amulet: false,
	leftWeapon: false,
	rightWeapon: false,
	armor: false,
	boots: false,
};
